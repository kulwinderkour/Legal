export default function Home() {
  return (
    <main
      id="main-content"
      className="flex min-h-full flex-1 items-center justify-center p-8"
    >
      <p className="max-w-prose text-center font-serif text-lg text-neutral-700 dark:text-neutral-300">
        Clause — scaffold in progress. Feature slices land in the steps that follow; see{" "}
        <code>README.md</code> for the build order.
      </p>
    </main>
  );
}
